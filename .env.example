# Copy to .env and fill in. NEVER commit .env or secrets/*.json
# Market-data / price-history root (daily CSVs: date,close,factor)
QLIB_LAB_DIR=C:\path\to\your\price-data-repo
# Optional research dependencies
MACRO_GPU_LAB_DIR=C:\path\to\macro-research-repo
# Optional broker market-data keys (READ-ONLY data use only; no trading client)
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
